meta:
  id: "performance.CachingStrategy"
  version: "0.1.0"
  status: "draft"
  authors: ["@stillrivercode"]

keyword: "CachingStrategy"
namespace: "performance"
brief: "The strategy used for caching data."
tokens_saved: 9

definition:
  description: |
    Defines the caching strategy for the application, including what to cache, where to cache it, and for how long.
  parameters:
    - name: "cacheType"
      type: "string"
      required: false
      default: "in-memory"

examples:
  - input: "CachingStrategy"
    expands_to: "Implement an in-memory caching strategy."
  - input: "CachingStrategy[cacheType=redis]"
    expands_to: "Implement a Redis caching strategy."

tests:
  - description: "Ensures the cache is being used."
    input: "CachingStrategy"
    should_include: ["cache", "get", "set"]

related: ["DatabaseSchema", "PerformanceBestPractices"]