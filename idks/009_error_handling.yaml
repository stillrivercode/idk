meta:
  id: "quality.ErrorHandling"
  version: "0.1.0"
  status: "draft"
  authors: ["@stillrivercode"]

keyword: "ErrorHandling"
namespace: "quality"
brief: "The strategy used for handling errors."
tokens_saved: 8

definition:
  description: |
    Defines a consistent strategy for handling and reporting errors throughout the application.
  parameters:
    - name: "errorFormat"
      type: "string"
      required: false
      default: "json"

examples:
  - input: "ErrorHandling"
    expands_to: "Implement a centralized error handling mechanism that returns errors in JSON format."
  - input: "ErrorHandling[errorFormat=xml]"
    expands_to: "Implement a centralized error handling mechanism that returns errors in XML format."

tests:
  - description: "Ensures errors are caught and handled."
    input: "ErrorHandling"
    should_include: ["try", "catch", "error"]

related: ["LoggingAndMonitoring"]