meta:
  id: "data.DatabaseSchema"
  version: "0.1.0"
  status: "draft"
  authors: ["@stillrivercode"]

keyword: "DatabaseSchema"
namespace: "data"
brief: "Defines the structure of the database."
tokens_saved: 5

definition:
  description: |
    Outlines the tables, columns, relationships, and constraints for the application's database.
  parameters:
    - name: "databaseType"
      type: "string"
      required: false
      default: "PostgreSQL"

examples:
  - input: "DatabaseSchema"
    expands_to: "Create a database schema for a PostgreSQL database."
  - input: "DatabaseSchema[databaseType=MySQL]"
    expands_to: "Create a database schema for a MySQL database."

tests:
  - description: "Ensures the schema includes a users table."
    input: "DatabaseSchema"
    should_include: ["users", "table"]

related: ["UserAuthentication"]