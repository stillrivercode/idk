meta:
  id: "ops.LoggingAndMonitoring"
  version: "0.1.0"
  status: "draft"
  authors: ["@stillrivercode"]

keyword: "LoggingAndMonitoring"
namespace: "ops"
brief: "Handles logging and monitoring of the application."
tokens_saved: 12

definition:
  description: |
    Implements a comprehensive logging and monitoring solution to track application health and performance.
  parameters:
    - name: "logLevel"
      type: "string"
      required: false
      default: "info"

examples:
  - input: "LoggingAndMonitoring"
    expands_to: "Set up logging and monitoring with an info log level."
  - input: "LoggingAndMonitoring[logLevel=debug]"
    expands_to: "Set up logging and monitoring with a debug log level."

tests:
  - description: "Ensures logs are being captured."
    input: "LoggingAndMonitoring"
    should_include: ["log", "capture", "monitor"]

related: ["ApiGateway", "ErrorHandling"]